<%- include('./partials/_header.ejs')  %>

<% places.forEach((place)=>{ %>
    <p><%= place.place  %></p>
    <form action="/addadmin/DelPlace/<%= place.id %>?_method=DELETE" method="post">
        <button>刪除</button>
    </form>
<% }) %>